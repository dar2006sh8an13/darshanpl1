#include<stdio.h>
#include<stdlib.h>
#include<conio.h>
int main(void)
{
    int balance=1,00,000,withdraw,deposite;
    operation=input("do you want to deposite or withdraw?(enter deposited or withdraw )")
    if(operation=="deposite")
    {
        float(input("enter the amount to deposite :"));
        balance+=deposite amount
        printf("amount deposited :₹%f",deposited amount);
        printf("new account balance :₹%f",balance);
    }
    else if{(operation=="with draw")}
    float(input("enter the amount to be withdraw:"))withdraw amount<=balance;
    {
        printf("withdrawn amount:₹%f",withdrawn amount)
        printf("new account balance :2%f",balance);
            }
            else;
        {
            printf("insufficent=");
        }
        else;
        {
            printf("invalid");
        }
}